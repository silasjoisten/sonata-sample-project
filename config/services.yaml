# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    sonata.media.admin.media.controller: 'SilasJoisten\Sonata\MultiUploadBundle\Controller\MultiUploadController'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
            - '../src/Tests/'

#    Sonata\Doctrine\Adapter\AdapterChain: ~
#
#    sonata.doctrine.model.adapter.chain:
#        alias: 'Sonata\Doctrine\Adapter\AdapterChain'
#
#    Sonata\MediaBundle\Admin\ORM\MediaAdmin:
#        arguments:
#            $code: ''
#            $class: '%sonata.media.admin.media.entity%'
#            $baseControllerName: 'sonata.media.controller.media.admin'
#        tags:
#            - { name: sonata.admin, manager_type: orm, group: admin, label: Media }
#
#    sonata.media.admin.media:
#        alias: 'Sonata\MediaBundle\Admin\ORM\MediaAdmin'
#
#    Sonata\MediaBundle\Admin\GalleryAdmin:
#        arguments:
#            $code: ''
#            $class: '%sonata.media.admin.gallery.entity%'
#            $baseControllerName: 'sonata.media.controller.gallery.admin'
#        tags:
#            - { name: sonata.admin, manager_type: orm, group: admin, label: Gallery }
#
#    sonata.media.admin.gallery:
#        alias: 'Sonata\MediaBundle\Admin\GalleryAdmin'
